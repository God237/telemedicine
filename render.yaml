services:
  - type: web
    name: telemed-connect
    runtime: php
    plan: free
    buildCommand: composer install
    startCommand: php -S 0.0.0.0:10000
    envVars:
      - key: PORT
        value: 10000